#!/bin/bash
# Health check for service
check_service health
sleep 5
# wait for healthy,max_attempts=3
# exit 0
}

 elif
                echo "Service $service is healthy"
                exit 0
            elif [? -n  == " -nech: the logic."
                else
                echo "Service ${name} is healthy"
                else
                sleep 5 before continuing..."
        fi
    else
    # exit 0 (healthy)
    else
    echo "Skipping health check"
        exit 0
    fi

